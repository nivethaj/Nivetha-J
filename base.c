#include <stdio.h>
int main()
{
int base,exp;
{
printf("Enter a base number: ");
scanf("%d", &base);
}
{
printf("Enter an exp: ");
scanf("%d", &exp);
}
{
while(exp!=0)
{
result *= base;
--exp;
}
return 0;
}
